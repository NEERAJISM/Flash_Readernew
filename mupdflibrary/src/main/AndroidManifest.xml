<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.artifex.mupdfdemo">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity android:name=".MuPDFActivity">
            <intent-filter>
                <action android:name="com.knightandday.neeraj.flash_reader.notes.Notes.intent.action.Launch" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".SearchableDictionary"/>
        <activity android:name=".DictionaryProvider">
            <intent-filter>
                <action android:name="com.knightandday.neeraj.flash_reader.notes.Notes.intent.action.Launch" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>
</manifest>
